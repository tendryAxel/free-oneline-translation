FROM libretranslate/libretranslate

# (Optionnel) expose à nouveau le port, utile pour documentation
EXPOSE 5000

# (Optionnel) si tu veux modifier l'entrée ou ajouter des fichiers, tu peux ici

# L'entrée par défaut est déjà bonne, donc pas besoin de CMD ou ENTRYPOINT

